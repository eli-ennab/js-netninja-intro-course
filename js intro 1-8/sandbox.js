// alert('hello world');
// console.log('1')
let age = 25;
let year = 2019;

// log things to console
console.log(age, year);

age = 30;
console.log(age);

const points = 100; // So that the value cannot be overridden
// points = 50 creates error if const
console.log (points);

var score = 75; // Older way to create variables
console.log (score);

// Javascript data types
    // Number:      1, 2, 3, 100, 3.1 etc.
    // String:      'hello, world'
    // Boolean:     true / false
    // Null:        explicitly set a variable without value
    // Undefined:   (automatically) for variables that has not yet been defined
    // Object:      complex data structures such as Arrays, Dates, Literals
    // Symbol:      used with objects

